import 'package:flutter/material.dart';

class FluroPage1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("FluroPage1")),
      body: Center(child: RaisedButton(onPressed: () {
        
      })),
    );
  }
}
